let alturaEnCm = 168;
let alturaaEnMts = 1.68;
let peso = 70.68;
let pesoRoundedUp = Math.ceil(peso);
let pesoRoundedDown = Math.floor(peso);
const max_valor_JS = Number.MAX_VALUE + 1;
const isEquals =  (max_valor_JS +  1) === max_valor_JS;
console.log(isEquals);
